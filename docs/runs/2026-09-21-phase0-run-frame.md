# Phase-0 foreman run frame

Director: Astra. Authority: Steven's takeover instruction; V1.3 plan Phase 0; Sol Pro roadmap sections 9–11. Starting exact state SHA: 94f7f9116f27be0b7604906c774a1e03663c768e; code baseline: 3ff37348f88750470be9f1ccda193b3ff31fb9c3.

## Predicate
P0-1 recovery campaign, P0-2 misspecification injection and P0-3 negative controls land on reviewed branches with a fresh Sol-audited exit-gate verdict. The code merge remains parked for Steven, SHA-first. The gate must recover synthetic truth within predeclared tolerances and expose non-identifiability. A failed scientific gate is reported, never relabelled PASS. Nothing fits real Jersey data before this passes.

## Budget and routing
Five iterations: (1) specification + P0-1; (2) P0-2; (3) P0-3; (4) integration, local mirror and fresh Sol independent exit audit; (5) bounded corrective/re-audit if needed. No more than three implementation attempts and two scientific consults per unit. Initial consult charged to P0-1; infrastructure failures are recorded separately. Final terra trail audit is mandatory closeout, not an implementation iteration. No token budget has been requested or imposed. Exhaustion preserves the original predicate and parks remaining scope honestly.

Implementation = gpt-5.6-luna xhigh, separate worktree per unit. Scientific specification and independent review = gpt-5.6-sol high, fresh read-only detached clones; spec author is not the exit judge. Trail audit = gpt-5.6-terra. Director writes briefs, reviews and verifies; no implementation beyond a one-line obvious fix.

## Unit contracts
- P0-1: intended-observation synthetic truth recovery across at least five distinct process seeds; joint fitted-dimension problem bounded to 3–5, with no fitted biological timing or route multipliers. Coverage/bias per dimension, deterministic evidence and explicit unsuccessful/non-identified results. Sol predeclares numerical design before Luna implements or runs any campaign.
- P0-2: prescribed wrong delay and ascertainment assumptions, same frozen recovery protocol, explicit degradation and failure-mode evidence. Depends on the kept P0-1 harness.
- P0-3: prescribed unidentifiable parameter negative control, DATA-9 per-factor evidence, no falsely precise estimates. Depends on the kept P0-1 harness; separate branch and scope from P0-2.
Nine-field implementation briefs are finalized from the filed Sol specification. No placeholder numeric choices may reach an executor. Workload stays ci-mode within the spec's hard cap; full-scale runs require Steven.

## Verification and write-back
Use the exact .github/workflows/ci.yml verify job locally. File all command transcripts and executor final reports with session IDs. Director full mirror must PASS on the integration SHA before push and independent review. A changed head voids the verdict. Keep raw evidence until trail audited; frozen data and existing evidence are immutable. Every trail row uses fm.sh log; each write-back reconciles RUN, FRONTIER, GATES and the trail, with a staleness sweep. State-only commits use fm.sh sync. G5 default remains preserve branches.

After Phase 0, park Phase-1 era/holdout/model-owner rulings in GATES with explicit defaults. No implicit era selection, real-data fit or merge authorization.