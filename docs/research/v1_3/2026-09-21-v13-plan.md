# V1.3 plan — first named-pathogen Jersey calibration (COVID era)

**Written:** 2026-09-21, Fable (director), planned with Steven in chat; the forward authority remains `docs/audits/2026-09-01-solpro-deep-audit-BLOCKED.md` §9–§11 — this plan operationalises it and never overrides it. §11's cut list is binding on every unit below. Director hand-over target: GPT-6 Astra (`$foreman` in a `codex -m gpt-6-astra` session in this repo); everything here is executable through the standard loop (luna@xhigh executors, Sol@high independent reviews, terra trail audits, merges Steven-gated).

**Starting state:** `main` code baseline `3ff3734` (G28: V1.2.1 engineering tail merged). V1.2 evidence foundation PASSED its exit gate. Validation run PASS 2026-09-19 (38.2-min ensemble, hashes exact). Calibration machinery honesty landed (DATA-8/9 + correctives): falsifiable held-out gate, real 2-D profiles, per-factor argmin shifts.

## Resume update — 2026-09-22

**G30 outcome:** correction `367f0324685437c4d2ed4aa0ae4878229da09839` passed Sol
bounded numerical review with no findings. Local gate passed under the existing
base-confirmed job-ordering flake rule (443 passed/4 skipped/1 known failure;
remaining verify steps passed). No Phase-0 campaign or scientific verdict.
See `docs/audits/2026-09-22-phase0-g30-review-sol-PASS.md`. G29 remains pending.

Steven subsequently instructed "alright keep iterating and working away".
The prepared G30 extension is released for one Luna correction and one Sol
review; G29's scientific reporting ruling remains pending. RUN/GATES own live
worker state. The stop update below records the prior closeout, not the current
authorization. No scientific thresholds, campaign permissions or merge rules change.

## Progress update — 2026-09-22 (Astra; Steven requested stop)

The original phases and scientific exit requirements below remain unchanged.
The Phase-0 declaration is frozen at `a4b3200967f34e449bd7ffd4f589a8d30e841e95`
(document SHA-256 `ef67fe49903c3984ca98679eb0470878bc25523baca3bc23a63e4ae7d983a104`).
P0-1 reached local candidate `b5ef032e29c577bce634ce0933b2d7d316ac562e` after
three Luna attempts. The local verification mirror passed 432 tests with four
skipped, but Sol found a declared-decimal inclusive-boundary defect; earlier
acceptance was withdrawn. The branch is held locally, not merged or pushed.

G30 requests one bounded corrective attempt and independent review; G29
requests the P0-2 unknown/tied-result reporting ruling. Neither is approved.
P0-2/P0-3 and the all-arms campaign have not run; the scientific exit gate is
unestablished. Terra's trail audit and director disposition are filed.
See [the closeout handoff](../../handoff/2026-09-22-phase0-stop.md) and
[RUN.md](../../../.claude/RUN.md) for exact paths and resume conditions.
Nothing is in flight; no automatic continuation is authorized by this closeout.

## Phase 0 — the REAL V1.2.1 exit gate: synthetic recovery and identifiability (audit §V1.2.1)

The roadmap used "V1.2.1" loosely for the engineering batches; the audit's V1.2.1 milestone — *prove the machinery recovers known parameters before fitting Jersey data* — has not run as a gated campaign. Nothing in V1.3 fits real data until this gate passes.

Units (each foreman-able, ci/scaled-mode):
- **P0-1 recovery campaign:** synthetic observations from known parameters (the intended observation model), recovery across ≥5 process seeds, coverage/bias table for each fitted dimension.
- **P0-2 misspecification injection:** fit under plausible wrong assumptions (wrong delay, wrong ascertainment regime); document where recovery degrades — failure modes are deliverables, not embarrassments.
- **P0-3 negative controls:** a parameter that should be unidentifiable from the synthetic data must come back flagged non-identified (the DATA-9 per-factor machinery is the instrument), never falsely precise.
- **Fitted-dimension ceiling (binding, §V1.2.1):** 3–5 effective dimensions — global transmission scale, seeding (initial prevalence or timing), broad import pressure if the era needs it, 1–2 ascertainment/regime parameters. Route multipliers, behavioural effects, natural-history timing stay FIXED to declared evidence (§11 forbids fitting them).

**Exit gate (Sol-audited):** synthetic truth recovered within predeclared tolerances; non-identifiability exposed, not hidden. Verdict format per handoff §10.8.

## Phase 1 — era selection + observation-model synthesis (model-owner ruling, written before any code)

A written synthesis (this directory), ruled by Steven or under his delegation, declaring:
- **The COVID era window** (recommended: first wave 2020 spanning the serosurvey fieldwork, or the well-instrumented winter 2020–21 — choose only after re-reading the frozen daily surveillance/testing tables for regime stability; the 2020 serosurvey must fall inside or adjacent to the window because it anchors cumulative infection).
- **Training vs holdout windows, PREDECLARED** and hashed into the synthesis before the first fit.
- **Observation regimes:** explicit testing/reporting regime boundaries from the frozen tables (`TestsTotalNegativeTests` stays excluded; positivity used only where the dictionary supports it).
- **Fixed-parameter table:** every biological timing parameter (latent/infectious/incubation) fixed from cited external evidence with locators — never estimated from Jersey aggregates (§V1.3 design rules).
- **NPI timeline fixture:** hand-reconstructed, dated, cited gov.je events with `evidence_source_id`s (OWID has no Jersey stringency data — known since 2026-09-03). One data-unit builds the frozen fixture; the synthesis rules which events enter the model and how (as observation-regime markers and/or scenario dates — NOT as fitted intervention multipliers, §11).
- **Import process:** broad import pressure only, using the frozen travel/arrivals tables; imported vs local separation at the resolution the data support, no more.

## Phase 2 — fit infrastructure (engineering units, exact-equivalence gates where they touch shared code)

- **P2-1 baseline model:** the declared simple comparator (e.g. a fitted SEIR or renewal curve on the same training window) the calibrated model must beat on holdout — required by the exit gate, so it is built first, not retrofitted.
- **P2-2 joint objective:** cases + tests/positivity + serology comparison per the dictionary's semantics (suppressed cells enter as censored, never zeros); serology constrains cumulative infection.
- **P2-3 calibration driver:** the predeclared grid/optimiser over the Phase-0 proven dimensions on the frozen tables; PERF-5 (parallel predeclared grids) lands here IF profiling of the actual workload demands it — profile first (§11: no surrogate/emulator complexity before the direct problem is profiled).
- **P2-4 run/artifact contract:** calibration artifacts carry training/holdout hashes, fixed-parameter table hash, and the synthesis document's hash — a cold-start auditor reproduces the fit from frozen inputs alone (V1.2 discipline extended to fits).

## Phase 3 — the calibration + validation campaign

1. Fit on training window (Steven authorises the expensive runs; ensemble economics: ~38 min per 44-replicate full-scale run makes replicate-based fit diagnostics affordable).
2. **Validation battery (§V1.3, all required):** held-out temporal prediction beats the baseline · serology cross-check · residual checks at regime boundaries · sensitivity to alternative plausible observation assumptions · parish/age comparisons only if unused in fitting and denominator-sound.
3. Independent Sol audit of the whole chain from frozen snapshots to verdict.

**Exit gate (verbatim from the authority):** the calibrated model beats the declared baseline on held-out targets, maintains reconciliation/conservation, and presents parameter and model uncertainty without unsupported Jersey-specific precision. Only after this may any output say "calibrated".

## V1.3.1 and beyond (pointers, not plans)

- **V1.3.1:** uncertainty decomposition — process / parameter / observation / scenario / discrepancy reported separately, labels per the authority (§V1.3.1); ≥40 replicates for 2.5/97.5 process quantiles, count set by convergence diagnostics.
- **V1.4:** structural enrichment only where it changes a decision-relevant result and has frozen evidence (candidate list in the authority).
- **V2:** named-pathogen depth (infectiousness profile, waning immunity, severity pathway, infected-traveller state) — gated on V1.3's exit, §10.
- Deferred mechanism questions parked during the scientific corrections: `activity_cv` on non-community routes; workplace persistence; retiring the `m4_identity_edge` legacy-token shim in a declared M4 migration.

## Sequencing and budget guidance for the director

Phase 0 is one foreman run (3 units + audit). Phase 1 is synthesis + one data unit (NPI fixture) — the synthesis needs model-owner rulings (park in GATES with defaults). Phase 2 is one to two foreman runs. Phase 3 is authorised-run territory. Suggested gates: era/holdout declaration (Steven), each merge (Steven), the Phase-3 run authorisations (Steven).

## Operational notes for the incoming director (Astra)

- Cold start: `.claude/REPO-MAP.md` → `CLAUDE.md` → `.claude/FRONTIER.md` → `.claude/RUN.md` → `.claude/GATES.md` → `.claude/DIRECTOR.md` (binding) → this plan.
- Machine facts: WSL loop home `~/jsy_disease_sim`; detached-session launch pattern and the `wsl -- bash -c` quoting rules in RUN.md/DIRECTOR lessons; local CI mirror is the gate. GitHub CI remains prohibited by Steven. Correction 2026-09-22: 12 early state pushes unintentionally triggered Actions; evidence and costs-unknown disclosure are in `docs/runs/2026-09-22-phase0-unintended-ci-correction.md`. Every state log/sync commit must include `[skip ci]`.
- Codex auth: rotated 2026-09-20; WSL copy synced. Do not run Windows and WSL codex concurrently.
- Known flake: `test_prov_job_ordering::test_missing_head_request_fails_alone…` (timing race; Steven ruled won't-fix-now). Confirm-on-base then disregard in reviews.
- Filter handling update 2026-09-22: Steven reported repeated Codex biological-content display warnings. Worker logs did not establish their cause. The earlier retry advice is superseded: do not bypass or disguise content to evade a filter. Preserve status, inspect the notice and use only a supported recovery path. This run's implementation retries corrected reproduced code defects; the recorded budgets remain authoritative.
